#!/home/yisenxu/rabeya/mini-swe-agent-ci-based/results/think_repair_deepseek/8efb5578f2c0b9ad8e539bb79ff522c50d3bb771/testbed/.venv-docsgpt-426/bin/python3

import docx2txt

if __name__ == '__main__':
    import sys
    args = docx2txt.process_args()
    text = docx2txt.process(args.docx, args.img_dir)
    output = getattr(sys.stdout, 'buffer', sys.stdout)
    output.write(text.encode('utf-8'))
